node_modules/
out/
result
result-*
.direnv/
.cache/
.env
.env.local
*.local.md
local.env
.worktrees/
.fallow/
.tmp/
.resession.json
/.nix-bin
/.pi/git
/.tanstack/tmp
.android-sdk/
dist/
clients/android/app/src/main/assets/portal/

# Device secrets are supplied out of band (see nix/wifi.nix).
*.wifi.env
wifi.env

# korrid writes its runtime state beside the binary when no state directory is
# set, and that state holds the device private key. A test run in a checkout
# must never be able to stage one.
korri-state/
*.key
