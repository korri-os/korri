diff --git a/src/nvhttp.cpp b/src/nvhttp.cpp
index 0ae83ac..07fe128 100644
--- a/src/nvhttp.cpp
+++ b/src/nvhttp.cpp
@@ -770,6 +770,9 @@ namespace nvhttp {
           std::getline(std::cin, pin);
 
           getservercert(ptr->second, tree, pin);
+          // The response guard sends the certificate reply. Do not fall
+          // through to lookup with uniqID, which was moved into the session.
+          return;
         } else {
 #if defined SUNSHINE_TRAY && SUNSHINE_TRAY >= 1
           system_tray::update_tray_require_pin();
